on: 
  push:
    branches:
      - build

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: release_build 
        run: |
          pwd
          sudo apt-get install dvipng dvisvgm
          pip install -r requirements.txt
          make gh-release
 